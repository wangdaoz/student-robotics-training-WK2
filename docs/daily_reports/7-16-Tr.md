## Today's Goal
   ● Install the appropriate ROS 2 distribution for your operating system, run official verification examples

   ● Create a workspace, and write a setup guide that another engineer can follow
## Completed
    ● Choose and document the ROS 2 distribution and platform.

    ● Install ROS 2 using the official documentation.

    ● Run official talker/listener examples.

    ● Create robot_ws with a src directory.

    ● Build the empty workspace with colcon.

    ● Source the workspace and verify the environment.

    ● Record exact commands, errors, fixes, and expected output in docs/ros2_setup.md.

    ● Create a simple architecture diagram showing workspace, package, node, topic, publisher, and subscriber.

## Problems Encountered
   
    1. After inputting  "sudo add-apt-repository universe", system asks me: adding component(s) 'universe' to all repositories?

    2. After opening two terminals to verify with the official talker/listener demo (matches Milestone 2.2's acceptance criteria),
       I didn't know how to exit a terminal.

    3. During the installation, I did not understand most commands.

## How I Solved Them
    Problem 1, I asked the claude chat, then I know I should confirm with yes response. I typed 'y' and hit "Enter"

    That prompt is just "add-apt-repository" asking you to confirm before it edits your apt sources list (usually /etc/apt/sources.list or the files under /etc/apt/sources.list.d/) to add the universe component alongside main. It's a standard, safe step — universe is Canonical's community-maintained package repo, and several ROS 2 dependencies live there, so you need it enabled for the install to work.

    Problem 2, I also asked claude chat, it answers me to press "Ctrl+C" in each terminal: sends an interrupt signal that cleanly stops the running ROS 2 node.

    Problem 3, I asked chatgpt to explain all of these commands. For each command, I understood it with the help of explaination. Then, I took notes in "ros2_setup.md"

## Claude Code Usage

   For each task, I asked the Claude code to help me because I didn't know how to do these tasks independently. Each task, I asked claude how to do it. The claude code answered my questions in detail.


## What I Learned
    
   1. before installing ROS 2 in WSL, I should know what Ubuntu version my WSL is running. use command <lsb_release -a>

   2. set locale
      A locale is a collection of settings that determine how programs display and process information according to a particular language and region.

      A locale specifies things such as:

       Language (English, Chinese, French, etc.)
       Country/Region (US, UK, China, Canada, etc.)
       Character encoding (UTF-8, ASCII, etc.)
       Date and time format
       Currency format
       Number format
       Sorting (alphabetical order)

    3. source
       executes the target file in current shell; Reloads your updated file right away; The current terminal gains same ROS configuration without needing to restarting

    4. colcon
               ● finds all packages in target directory

               ● Determines the dependency order

    5. After finishing installation, I should also verify the installation.

## Plan for Tomorrow
    Create first ROS 2 package