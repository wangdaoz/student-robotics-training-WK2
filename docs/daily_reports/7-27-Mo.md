■ Milestone
  
   2.5 Build a Reusable ROS 2 Onboarding Example

■ Current objective

       Polish student_robotics into a reusable example. Add a launch file, clear configuration or parameters, architecture documentation, tests, and a troubleshooting section.

■ Work completed

                    •	☐ Launch file
                    •	☐ Configurable behavior
                    •	☐ Architecture diagram
                    •	☐ Troubleshooting guide
                    •	☐ Verification procedure
                    •	☐ Final merged main branch
                    •	☐ Demo


■ Commands or tests run

          Task 1: Add one launch command that starts publisher and subscriber together

                  access to the directory of your workspace
                  <cd ~/<Username>/student-robotics-training-WK2/robot_ws>
                    Note: replace <Username> with your actually username

                  Add a launch file:
                   <touch src/student-robotics/launch/status_demo.launch.py>
                     --touch create a new, empty file with a given name

                  Rebuild the package and resource it in current shell
                   <colcon build --packages-select student_robotics>

                  read and execute the commands in given scripts inside the current shell
                  the script configures your shell by updating several environment variables.
                   <source install/setup.bash>

          Task 2: Make at least one behavior configurable, such as publish rate or topic name

               Suppose you want to make both publish rate and topic name configurable and all relevant files are configured:
                  <colcon build --packages-select student_robotics>
                  <source install/setup.bash>

          Task 5: Add a verification script
                  
                  1. create a bash file: <touch ~/student-robotics-training-WK2/tests/verify_demo.sh>

                  2. Suppose you're in your repo and finished editing the scripts,
                  <chmod +x tests/verify_demo.sh>
                  --chmod
                     change file's permissions

                  -- '+x'
                     add the execute permission
                     Without it, the file is just text — Linux won't let you run it directly, and ./tests/verify_demo.sh will fail with "Permission denied." After chmod +x, the file is marked executable, and since it starts with #!/bin/bash (the shebang line), the system knows to run it with bash when you invoke it directly

        Tests:
          
          Task 1: 
             Case 1: in built-in terminal
             <ros2 launch student_robotics status_demo.launch.py>

             -- ros2 invokes the ROS 2 command-line interface

             -- launch tell the ROS 2 to run a launch file

             -- <packagename>: the package containing the file

             -- <launch_file> is the python launch file to execute

    
             Case 2: in a new terminal window
            
             <env -i HOME="$HOME" TERM="$TERM" bash --norc --noprofile> (optional)

              --env a Linux utility for working with environment variables

              -- '-i' ignore the current environment

              -- HOME="$HOME"
                   "Create a new environment variable named HOME whose value is the same as my current HOME.

              -- TERM="$TERM"
                     copies your current terminal type.

              -- bash
                  starts a new bash shell

              -- '--norc'
                Normally, when Bash starts interactively, it reads: ~/.bashrc

                '--norc' tells Bash: Do not read '~/.bashrc'
                So none of those customizations are applied.

              -- '--noprofile' 
                  When Bash starts as a login shell, it normally reads one of: '~/.bash_profile' or '~/.profile'

                  These files often contain commands like:
                                         export PATH=...
                                         source /opt/ros/jazzy/setup.bash

                 '--noprofile' tells bash: "Ignore all login startup files."

             <source /opt/ros/jazzy/setup.bash>
               Read and execute the given script in current shell

               Role: Configure my current terminal to use the system-wide ROS 2 Jazzy installation by executing its setup script, so ROS 2 commands (ros2), packages, libraries, and Python modules are available in this shell."

              <source ~/robot_ws/install/setup.bash>
                 Read and execute the setup.bash script generated by colcon in the current shell so that this terminal knows about all the ROS 2 packages, executables, launch files, and Python modules installed in my workspace."

             <ros2 launch student_robotics status_demo.launch.py>

              Open another terminal, source it as mentioned above, input following commands respectively

             <ros2 node list>

             <ros2 topic list>

             <ros2 topic hz /robot_status>
            
             <ros2 param get /status_publisher publish_rate>
          
           Task 2:
                 1. input the commands:
                  <ros2 launch student_robotics status_demo.launch.py publish_rate:=2.0 topic_name:=robot_status_test>
                  in current terminal

                 2. in a new terminal:
                      <source /opt/ros/jazzy/setup.bash>

                      <source ~/robot_ws/install/setup.bash>

                      <ros2 topic list> or <ros2 topic hz /robot_status_test> or <ros2 topic echo /robot_status_test>
                       
                      If messages show up on rename topic and reset publish rate, both nodes correctly picked up the same override.

          Task 5:
                   <./tests/verify_demo.sh>
                


■ Blockers
    Task 1:
        issue 1:
           After I edited the launch file, I forget how to test the file in a genuinely fresh terminal
        
    Task 2:
          In milestone 2.4, in stretch goal section I had finished a foundation of this task but I was still confused on how to do it and how to make the 'topic' configurable

    Task 5:
         At the begining, I didn't understand the requirement of this task.

         Next, I didn't understand some commands in the script: "verify_demo.sh"

         Then, after I read the contents in the script, I discovered, my executable file for the publisher is a little different from the script. So I was not sure that this script is correct or whether I should modify something in the script.
            
     

■ How I investigated
      Task 1:
          I asked Claude Code the question: "How to test it in a genuinely fresh terminal?", it answered me with plenty of details;
          Then, I understood those contents with previous knowledge I had grasped; Finally, I wrote them into my README.md file's "Testing" section.

      Task 2:
          I asked Claude Code tow questions respectively:
            "For the second engineering task: Make at least one behavior configurable, such as publish rate or topic name. In previous milestone, I have made the 'publish rate' a parameter but I still be confused on how to do this task. Can you explain it?"
          after it answered me how to do then I asked another question: "if I want to make the 'topic' configurable, how to do it?"

          Then it told me that declare it in the publisher node and declare it in the subscriber node with relevant python3 codes.

          Some codes I didn't understand so I asked Chatgpt to explain them with details. Next, I understood these codes again and took notes in my troublshooting file.

     Task 5:
         I asked Claude code: "In fifth task, it requires me "Add a verification script, automated test, or rigorous manual acceptance procedure." Can you explain how to do it?"

         It answers: "Task 5 gives you three options, and you only need one, not all three. Which one makes sense depends on how much time you want to spend versus how much confidence you want it to give." Next, it introduced how to do each option with relevant details. After considered comprehensively, I choose to make a verification script.

         Then, I read contents in the script: "verify_demo.sh". Once I met commands I didn't understand, I asked Chatgpt to explain it and took nots in troubleshooting file.
         __________________________________________________________________________________________________________________________

         Next I asked CLaude code: "If I want to execute the "status_publisher_stretch.py", where should modify in script?"
         It answered: check two files: "setup.py" and "status_demo.launch.py".
           In "setup.py", register it as an executable (entry_points):
            Open robot_ws/src/student_robotics/setup.py and find the 'entry_points' block, check whether the codes <'status_publisher_stretch = student_robotics.status_publisher_stretch:main',> is existing.
            The format is: '<command_name> = <package>.<module_filename_without_.py>:<function_to_run>'.
            So if your file is at: robot_ws/src/student_robotics/student_robotics/status_publisher_stretch.py and it has a "def main(args=None):" function, the line above is correct.

         In "status_demo.launch.py", it reminded me to check the name of 'executable' in publisher node(publisher_node), it should match the entry_points name exactly. I did it and didn't find any problem.
        ___________________________________________________________________________________________________________________________
         Finally I asked last question: "Do I need to change the bash scripts?"
         It answered: No and explained:
          According to the script "verify_demo.sh", <ros2 node list | grep -q "/status_publisher">
          The script never references status_publisher_stretch directly. It:
           ● calls the launch file, which is where the executable name lives (executable='status_publisher_stretch')
           ● checks the ROS node name, which comes from name='status_publisher' in the launch file's Node(...) block — not the executable filename

           Since you kept name='status_publisher' when you switched executables (a good practice — it keeps the topic/node graph stable even as the underlying code changes), the node still shows up as /status_publisher in ros2 node list, and the script's checks pass exactly as before. That's the benefit of the script checking behavior (node present, topic present, rate correct) rather than implementation (which .py file is running).


■ AI tools used
     I used Claude code and Chatgpt to help me do tasks and answered my confusion.

■ Next action
      Weekend Engineering Wrap-up
